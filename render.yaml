services:
  - type: worker
      name: RideMatchBot
          runtime: python
              pythonVersion: 3.12
                  buildCommand: pip install -r requirements.txt
                      startCommand: python3 bot.py
                          region: oregon
